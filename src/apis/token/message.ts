enum Messages {
  INVALID_EXP_TOKEN = "token mey be invalid or expire.",
  INVALID_JWT = "invalid JWT token format",
  NOT_ACCESS = "you have no access",
  BLACK_LISTED = "token successfully blacklisted",
  AUTH_HEADER_NOT_FOUND = "authorization header not found",
}

export default Messages;
